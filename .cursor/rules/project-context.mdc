---
description: Project-wide context and constraints
alwaysApply: false
globs:
  - 'packages/**/src/**'
  - 'scripts/**'
---

# Project context

- **TypeScript** (Node LTS); build via `tsc` → `dist/`; templates via `scripts/build-templates.ts`
- **Tests:** Mocha + Sinon + Chai; `yarn test-packages` / `yarn coverage-packages`
- **Lint:** ESLint + Prettier; do not import from `dist/`

Overview: [`.agents/docs/AGENTS-overview.md`](../../.agents/docs/AGENTS-overview.md)
