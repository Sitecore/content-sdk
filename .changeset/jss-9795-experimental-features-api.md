---
'@sitecore-content-sdk/nextjs': minor
'create-content-sdk-app': patch
---

[experimental] Add experimental features visibility API for Next.js (editing-secret protected), with routes in all Next.js templates. Catalog lives in `src/experimental.json` (dummy entries for testing only; default is `[]`).
