export const command = ['generate-sdk', 'g-sdk'];

export const describe = 'Generates the SDK';

export const builder = {};

/**
 * Handler for the generate-sdk command.
 */
export function handler() {
  console.log('SDK is generating');
}
