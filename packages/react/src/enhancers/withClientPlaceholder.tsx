'use client';
import { withAppPlaceholder } from './withAppPlaceholder';

/**
 * withAppPlaceholder import for client-side context and client components
 */
export const withClientPlaceholder = withAppPlaceholder;
